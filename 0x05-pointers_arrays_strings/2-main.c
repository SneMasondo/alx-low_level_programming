#include "main.h"
#include <stdio.h>

int main(void)
{
    /* Testing the function _strlen */
    char *str = "Hello, world!";
    printf("Length of the string: %d\n", _strlen(str));
    return (0);
}
