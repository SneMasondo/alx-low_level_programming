#!/bin/bash
# File: 1-compiler

# Ensure the C file is provided
if [ -z "$CFILE" ]; then
  echo "Error: No C file specified."
  exit 1
fi

# Extract the base name of the file (e.g., main.c -> main)
OUTPUT="${CFILE%.c}.o"

# Compile the C file without linking
gcc -c "$CFILE" -o "$OUTPUT"

